PORT=3333

# Token aceito em Authorization: Bearer <token>.
# Precisa bater com VITE_API_TOKEN do front (web/.env.example).
AUTH_TOKEN=demo-token

# Simula a queda do (ERP) pra testar  o 503.
# SIMULATE_DOWNTIME=true
