{
  "name": "casecellshop-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=.env dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@vitest/ui": "^5.0.1",
    "tsx": "^4.20.6",
    "typescript": "~6.0.2",
    "vitest": "^5.0.1"
  }
}
